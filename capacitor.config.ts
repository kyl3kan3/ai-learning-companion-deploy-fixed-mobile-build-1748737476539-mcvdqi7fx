import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aiappbuilder.ailearningcompaniondeployfixedmobilebuild1748737476539mcvdqi7fx',
  appName: 'ai-learning-companion-deploy-fixed',
  webDir: 'dist',
  bundledWebRuntime: false,
  server: {
    androidScheme: 'https'
  }
};

export default config;